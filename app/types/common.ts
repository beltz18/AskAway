export interface Page {
  name: string
}

export interface THead {
  title?: string;
  description?: string;
  language?: string;
}

export interface loginInfo {
  email: string;
  password: string;
}